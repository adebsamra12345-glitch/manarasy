# ==========================================
# الأساسيات وإطار العمل (Core Framework & API)
# ==========================================
Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.1
django-environ>=0.11.2

# ==========================================
# قواعد البيانات (Database & PostGIS)
# ==========================================
# محول PostgreSQL الأساسي للتعامل مع قاعدة البيانات المركزية وقواعد المساجد
psycopg2-binary>=2.9.9

# ==========================================
# المصادقة والأمان (Security, Auth & 2FA)
# ==========================================
# لإدارة رموز JWT المذكورة في ملفات الأمان
djangorestframework-simplejwt>=5.3.1
# خوارزميات التشفير المطلوبة (Argon2 / Bcrypt) لحماية كلمات المرور
argon2-cffi>=23.1.0
bcrypt>=4.1.2
# مكتبات تفعيل المصادقة الثنائية 2FA
pyotp>=2.9.0
qrcode>=7.4.2

# ==========================================
# التخزين السحابي (Cloud Storage)
# ==========================================
# التكامل مع Amazon S3 لرفع الملفات والتسجيلات الصوتية والنسخ الاحتياطي
boto3>=1.34.14
django-storages>=1.14.2

# ==========================================
# التكامل والواجهات الخارجية (Integrations & APIs)
# ==========================================
# للاتصال ببوابة دفع "شام كاش"، ومحركات الإرسال (WhatsApp/SMS)، وواجهات المصحف الرقمي
requests>=2.31.0

# ==========================================
# المهام المجدولة (Background Tasks)
# ==========================================
# لجدولة النسخ الاحتياطي الشهري المدفوع وإدارة الإشعارات
celery>=5.3.6
redis>=5.0.1

# ==========================================
# ميزات الذكاء الاصطناعي (AI Assistant)
# ==========================================
# لميزات توقع معدل النسيان وتقييم النطق الصوتي في اشتراكات الـ 50$ و 500$
openai>=1.12.0
# (يمكن لاحقاً إضافة مكتبات متخصصة بالصوت مثل librosa أو SpeechRecognition إذا تم بناء محرك الصوت داخلياً)

# ==========================================
# خادم الإنتاج (Production)
# ==========================================
gunicorn>=21.2.0