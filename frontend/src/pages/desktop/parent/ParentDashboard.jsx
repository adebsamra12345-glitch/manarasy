/**
 * ParentDashboard
 * لوحة متابعة الطالب لولي الأمر
 */
const ParentDashboard = () => {
    return (
        <div style={{ padding: '2rem', textAlign: 'center' }}>
            <h2 style={{ color: 'var(--primary-green, #133315)', marginBottom: '0.75rem' }}>بوابة ولي الأمر</h2>
            <p style={{ color: '#888' }}>لوحة متابعة الطالب لولي الأمر</p>
        </div>
    );
};

export default ParentDashboard;
